# rag-chatbot-huggingface
Document-based chatbot API using Retrieval-Augmented Generation (RAG) with LangChain, HuggingFace embeddings, FAISS vector store and FastAPI. Implemented document chunking, semantic search, prompt-based generation and custom answer extraction logic.
